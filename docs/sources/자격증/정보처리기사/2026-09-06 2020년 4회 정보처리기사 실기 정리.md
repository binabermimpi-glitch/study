# 2020년 4회 정보처리기사 실기 정리

---

# 1. GoF 디자인 패턴 🔥🔥 매우 빈출

## 문제

디자인 패턴에 대한 설명이다. 빈 칸 안에 들어갈 가장 적합한 용어를 쓰시오.

디자인 패턴은 객체 지향 프로그래밍 설계를 할 때 자주 발생하는 문제들에 대해 재사용할 수 있도록 만들어놓은 패턴들의 모음이다.

에리히 감마, 리차드 헬름, 랄프 존슨, 존 블리시디스에 의해 개발 영역에서 디자인 패턴을 구체화하고 체계화시켰다.

GoF의 디자인 패턴은 생성, 구조, (   )의 3가지로 분류한다.

## 정답

**행위(Behavioral)**

---

# GoF 디자인 패턴

**Gang of Four**가 정리한 대표적인 객체지향 디자인 패턴이다.

GoF 디자인 패턴은 크게 세 가지로 나뉜다.

```text
생성 패턴
구조 패턴
행위 패턴
```

## ① 생성 패턴(Creational)

객체를 **어떻게 생성할 것인가**에 관련된 패턴이다.

대표:

```text
Singleton
Factory Method
Abstract Factory
Builder
Prototype
```

---

## ② 구조 패턴(Structural)

클래스와 객체를 **어떻게 조합하여 더 큰 구조를 만들 것인가**에 관련된다.

대표:

```text
Adapter
Bridge
Composite
Decorator
Facade
Flyweight
Proxy
```

지난번에 공부한:

```text
Facade Pattern
```

도 구조 패턴이다.

---

## ③ 행위 패턴(Behavioral)

객체들이 서로 **어떻게 책임을 나누고 상호작용할 것인지**와 관련된다.

대표:

```text
Observer
Strategy
State
Command
Iterator
Template Method
Mediator
Memento
Visitor
Chain of Responsibility
Interpreter
```

지난번 공부한:

```text
Observer Pattern
```

은 행위 패턴이다.

## 시험 암기

```text
생성
→ 객체 생성

구조
→ 객체 조합

행위
→ 객체 간 상호작용
```

---

# 2. 패키지 다이어그램 🔥 빈출

## 문제

UML을 이용한 다이어그램 중 아래 그림에 해당하는 다이어그램을 쓰시오.

## 정답

**패키지 다이어그램(Package Diagram)**

---

# 패키지 다이어그램

클래스, 유스케이스 등의 여러 모델 요소를 **패키지 단위로 그룹화하고 패키지 사이의 관계를 표현하는 UML 구조 다이어그램**이다.

```text
여러 요소
↓
패키지로 그룹화
↓
패키지 간 관계 표시
```

---

# UML 다이어그램 분류 🔥

UML 다이어그램은 크게:

```text
구조 다이어그램
행위 다이어그램
```

으로 나눈다.

---

# 구조 다이어그램(Structure Diagram)

시스템의 **정적인 구조**를 표현한다.

## ① 클래스 다이어그램

클래스의 속성, 연산 및 클래스 사이의 관계를 표현한다.

```text
클래스 구조 / 관계
→ 클래스 다이어그램
```

---

## ② 패키지 다이어그램

여러 모델 요소를 패키지로 그룹화하고 패키지 간 의존관계를 표현한다.

```text
그룹화
패키지 간 관계
→ Package Diagram
```

---

## ③ 복합체 구조 다이어그램

**Composite Structure Diagram**

클래스나 컴포넌트 내부의 복합적인 구조를 표현한다.

---

## ④ 객체 다이어그램

**Object Diagram**

특정 시점에 존재하는 객체와 객체 사이의 관계를 표현한다.

```text
클래스
→ 설계도

객체
→ 실제 인스턴스 상태
```

---

## ⑤ 컴포넌트 다이어그램

**Component Diagram**

소프트웨어 컴포넌트와 컴포넌트 사이의 관계를 표현한다.

---

## ⑥ 배치 다이어그램

**Deployment Diagram**

소프트웨어가 어떤 하드웨어/노드에 배치되는지를 표현한다.

```text
서버
하드웨어
네트워크
배치
→ Deployment
```

---

# 행위 다이어그램(Behavior Diagram)

시스템의 **동적인 동작과 상호작용**을 표현한다.

## ① 유스케이스 다이어그램

사용자 관점에서 시스템의 기능을 표현한다.

```text
Actor + 시스템 기능
```

---

## ② 활동 다이어그램

**Activity Diagram**

업무 처리 절차나 작업 흐름을 표현한다.

```text
업무 흐름
→ Activity
```

---

## ③ 시퀀스 다이어그램

**Sequence Diagram**

객체 사이에서 시간 순서에 따라 주고받는 메시지를 표현한다.

```text
객체
메시지
시간 순서
→ Sequence
```

---

## ④ 커뮤니케이션 다이어그램

**Communication Diagram**

객체 간 상호작용을 표현하되 **객체 간 구조적인 관계**를 중심으로 나타낸다.

> 예전 명칭으로 `Collaboration Diagram`이라고도 한다.

---

## ⑤ 상태 머신 다이어그램

**State Machine Diagram**

객체의 상태 변화와 생명주기를 표현한다.

```text
상태 변화
→ State Machine
```

---

# UML 초압축

```text
클래스
→ 클래스 구조

패키지
→ 요소 그룹화

배치
→ 하드웨어/서버 배치

유스케이스
→ 사용자와 기능

활동
→ 업무 흐름

시퀀스
→ 메시지 시간순서

상태
→ 객체 상태 변화
```

---

# 3. 샘플링 오라클 🔥 빈출

## 문제

일반, 업무용, 게임, 오락 등의 일반적인 업무에 사용하는 테스트 오라클로 특정한 몇 개의 입력 값에 대해서만 기대하는 결과를 제공해 주는 오라클을 쓰시오.

## 정답

**샘플링 오라클(Sampling Oracle)**

---

# 테스트 오라클(Test Oracle)

테스트 결과가 올바른지 판단하기 위해 **예상되는 결과를 제공하는 방법이나 기준**이다.

---

## ① 참 오라클(True Oracle)

모든 입력값에 대해 정확한 예상 결과를 제공한다.

```text
모든 입력
→ 정확한 결과
```

---

## ② 샘플링 오라클(Sampling Oracle)

일부 입력값에 대해서만 정확한 예상 결과를 제공한다.

```text
특정 몇 개 입력
→ 정확한 결과

나머지
→ 제공 X
```

---

## ③ 휴리스틱 오라클(Heuristic Oracle)

일부 입력에 대해서는 정확한 결과를 제공하고 나머지는 **휴리스틱 또는 추정값**을 사용한다.

```text
일부 정확
+
나머지 추정
```

---

## ④ 일관성 검사 오라클

**Consistent Oracle**

프로그램 변경 전과 변경 후의 결과가 서로 일관되는지 확인한다.

---

## 시험 구별

```text
모든 입력 정확
→ 참 오라클

일부 입력만 정확
→ 샘플링 오라클

일부 정확 + 나머지 추정
→ 휴리스틱 오라클

변경 전후 비교
→ 일관성 검사 오라클
```

---

# 4. 동등 분할 테스트 🔥🔥 매우 빈출

## 문제

테스트 기법 중 다음과 같이 입력값을 넣고, 출력값을 비교하는 테스트 기법을 쓰시오.

## 정답

**동등 분할 테스트(Equivalence Partitioning)**

또는

**동치 분할 테스트**

---

# 동등 분할

입력 데이터를 동일한 결과를 낼 것으로 예상되는 **유효 영역과 무효 영역으로 분할한 후 각 영역의 대표값을 테스트**하는 블랙박스 테스트 기법이다.

예:

```text
나이 입력 범위
1 ~ 100
```

분할:

```text
X < 1
→ 무효

1 ≤ X ≤ 100
→ 유효

X > 100
→ 무효
```

각 영역에서 대표값을 골라 테스트한다.

---

# 블랙박스 테스트 기법 🔥

## ① 동등 분할

```text
유효/무효 영역 분할
→ 대표값 테스트
```

## ② 경계값 분석

```text
경계 근처 값 테스트
```

예:

```text
0 ~ 10

-1
0
1
9
10
11
```

## ③ 원인-효과 그래프

입력 조건과 출력 결과 사이의 관계를 분석한다.

## ④ 오류 예측

테스터의 경험과 직관으로 오류가 예상되는 부분을 검사한다.

## ⑤ 비교 검사

여러 버전의 프로그램에 같은 값을 입력하여 결과를 비교한다.

### 암기

```text
유효/무효 그룹
→ 동등 분할

경계 근처
→ 경계값 분석
```

---

# 5. GROUP BY / COUNT 🔥🔥 매우 중요

## 문제

`<학생>` 테이블을 대상으로 요구사항을 적용하여 결과와 같이 출력하는 SQL문을 작성하시오.

### 요구사항

1. WHERE 구문은 사용하지 않고 SQL문을 작성하시오.
2. GROUP BY 구문과 집계함수를 반드시 사용하여 SQL문을 작성하시오.
3. 인용 문구를 사용 시 작은따옴표(`' '`) 사용 가능합니다.
4. AS 구문은 반드시 사용하여 작성하시오.

## 정답

```sql
SELECT 학과,
       COUNT(*) AS 학과별튜플수
FROM 학생
GROUP BY 학과;
```

---

# 실행 과정

## SELECT

```sql
SELECT 학과,
       COUNT(*) AS 학과별튜플수
```

학과 이름과 해당 학과에 속하는 튜플의 개수를 출력한다.

---

## COUNT(*)

```sql
COUNT(*)
```

행의 개수를 계산하는 집계 함수이다.

---

## AS

```sql
AS 학과별튜플수
```

출력되는 컬럼에 별칭(Alias)을 지정한다.

---

## GROUP BY

```sql
GROUP BY 학과
```

같은 학과의 데이터를 하나의 그룹으로 묶는다.

---

# 집계 함수 🔥

```text
COUNT
→ 개수

SUM
→ 합계

AVG
→ 평균

MAX
→ 최댓값

MIN
→ 최솟값
```

---

# WHERE / GROUP BY / HAVING

```text
WHERE
→ 그룹화 전 행 조건

GROUP BY
→ 같은 값끼리 그룹

HAVING
→ 그룹화 후 그룹 조건
```

---

# 6. 스니핑(Sniffing) 🔥 빈출

## 문제

스니핑에 대하여 간략히 서술하시오.

## 정답

**네트워크상에 통과되는 패킷들의 내용을 엿보는 행위이다.**

## 시험용 답안

> 네트워크를 통해 전송되는 패킷을 가로채어 그 내용을 몰래 분석하거나 정보를 탈취하는 공격이다.

## 핵심

```text
네트워크 패킷
+
도청
+
정보 탈취
→ Sniffing
```

---

# 보안 공격 비교

```text
Sniffing
→ 패킷 엿보기

Spoofing
→ 정보/주소 위조

Session Hijacking
→ 연결된 세션 탈취

Phishing
→ 사용자를 속여 정보 탈취
```

---

# 7. 가용성(Availability) 🔥🔥 매우 빈출

## 문제

정보보안의 3요소 중 가용성에 대하여 서술하시오.

## 정답

**인가된 사용자가 정보를 요구할 때 적시에 제공되어야 하는 원칙**

---

# 정보보안의 3요소 - CIA 🔥🔥

## ① 기밀성(Confidentiality)

인가된 사용자만 정보에 접근할 수 있어야 한다.

```text
허가된 사람만 볼 수 있음
```

---

## ② 무결성(Integrity)

정보가 허가되지 않은 방법으로 변경되거나 훼손되어서는 안 된다.

```text
데이터가 정확하게 유지
```

---

## ③ 가용성(Availability)

인가된 사용자가 필요할 때 언제든 정보나 서비스를 사용할 수 있어야 한다.

```text
필요할 때 사용 가능
```

---

## CIA 암기

```text
C = Confidentiality
    기밀성

I = Integrity
    무결성

A = Availability
    가용성
```

### 공격과 연결

```text
정보 유출
→ 기밀성 침해

정보 변조
→ 무결성 침해

DoS / DDoS
→ 가용성 침해
```

---

# 8. 데이터베이스 이상(Anomaly) 🔥🔥 매우 빈출

## 문제

데이터베이스의 이상(Anomaly)의 종류 3가지를 쓰시오.

## 정답

```text
삽입 이상
갱신 이상
삭제 이상
```

---

# 이상 현상

정규화가 제대로 이루어지지 않은 테이블에서 데이터 중복 때문에 발생하는 문제이다.

---

## ① 삽입 이상(Insertion Anomaly)

필요 없는 데이터까지 입력해야만 원하는 데이터를 삽입할 수 있는 현상이다.

예:

학생이 없는 학과 정보를 저장하고 싶은데 학생 정보까지 반드시 입력해야 하는 경우.

---

## ② 갱신 이상(Update Anomaly)

중복된 데이터 중 일부만 수정되어 데이터가 서로 불일치하게 되는 현상이다.

```text
같은 학과 이름이 여러 행
↓
한 행만 수정
↓
서로 다른 값 발생
```

---

## ③ 삭제 이상(Deletion Anomaly)

특정 데이터를 삭제할 때 필요한 다른 정보까지 함께 삭제되는 현상이다.

---

## 핵심

```text
삽입
→ 넣을 때 문제

갱신
→ 수정할 때 문제

삭제
→ 지울 때 문제
```

### 해결

```text
이상 현상 감소
→ 정규화
```

---

# 9. 즉시 갱신 회복 기법 🔥🔥 매우 중요

## 문제

데이터베이스 회복 기법에 관련된 설명에서 괄호에 들어갈 가장 알맞은 용어를 쓰시오.

다양한 데이터 회복 기법 중 (   ) 회복 기법은 로그를 이용한 회복 기법으로 데이터베이스에 대한 갱신 로그를 저장함으로써 회복에 대비한다.

(   ) 회복 기법은 트랜잭션이 실행 상태에서 변경되는 내용을 그때그때 바로 데이터베이스에 적용하는 기법이다.

(   ) 회복 기법은 장애가 발생하면 로그 파일에 기록된 내용을 참조하여 장애 발생 시점에 따라 Redo나 Undo를 실행하여 데이터베이스를 복구한다.

## 정답

**즉시 갱신(Immediate Update)**

---

# 즉시 갱신

트랜잭션이 완료되기 전이라도 변경 내용을 **즉시 데이터베이스에 반영**하고 그 내용을 로그에 기록한다.

장애가 발생하면:

```text
Undo
+
Redo
```

를 사용할 수 있다.

### 핵심

```text
즉시 DB 반영
+
로그 사용
+
Undo / Redo
→ 즉시 갱신
```

---

# 지연 갱신(Deferred Update) 🔥

트랜잭션이 완료되기 전에는 실제 데이터베이스에 변경 내용을 반영하지 않고 로그에만 기록한다.

트랜잭션이 완료된 후 반영한다.

### 회복

```text
Redo O
Undo X
```

왜?

완료 전에 DB 자체를 수정하지 않았기 때문이다.

---

# 즉시 갱신 vs 지연 갱신

```text
즉시 갱신
→ 완료 전 DB 반영
→ Undo + Redo

지연 갱신
→ 완료 후 DB 반영
→ Redo만
```

---

# 검사점 회복(Checkpoint Recovery)

트랜잭션 처리 중 특정 시점에 검사점을 설정하고, 장애 발생 시 검사점 이후의 로그를 중심으로 회복한다.

```text
Checkpoint
→ 복구해야 할 로그 범위 감소
```

---

# 그림자 페이징(Shadow Paging)

로그를 사용하지 않고 페이지의 복사본인 **그림자 페이지**를 유지한다.

```text
원본 페이지
+
Shadow Page
```

장애 발생 시 그림자 페이지를 이용하여 복구한다.

### 핵심

```text
로그 X
그림자 페이지 O
→ Shadow Paging
```

---

# 10. 프로세스 상태 🔥 빈출

## 문제

빈칸에 알맞은 프로세스 상태를 쓰시오.

## 정답

```text
1. 준비(Ready)
2. 실행(Running)
3. 대기(Waiting / Blocked)
```

---

# 프로세스 상태

## 준비(Ready)

CPU를 할당받으면 바로 실행할 수 있는 상태.

```text
CPU 기다리는 중
```

## 실행(Running)

CPU를 할당받아 현재 명령을 수행하고 있는 상태.

## 대기(Waiting / Blocked)

입출력 등의 특정 사건이 완료되기를 기다리는 상태.

```text
I/O 기다리는 중
```

---

# 핵심 흐름

```text
준비
↓ CPU 할당
실행

실행
↓ I/O 요청
대기

대기
↓ I/O 완료
준비
```

---

# 11. IPv6 🔥🔥 매우 빈출

## 문제

다음 설명에 해당하는 용어를 쓰시오.

* IPv4의 주소 고갈 문제를 해결하기 위하여 기존의 IPv4 주소 체계를 128비트 크기로 확장한 차세대 인터넷 프로토콜 주소
* 헤더의 내용을 확인하는 데 소요되는 오버헤드를 최소화하도록 설계
* 16비트씩 8부분, 128비트로 구성되며 콜론(`:`)으로 구분한다.
* 주소 분류로 유니캐스트, 멀티캐스트, 애니캐스트를 사용한다.

## 정답

**IPv6**

---

# IPv6 특징

```text
128bit
16bit × 8
16진수
콜론(:)
```

주소 방식:

```text
Unicast
Multicast
Anycast
```

### 중요

```text
IPv6
→ Broadcast 없음
```

---

# IPv4

```text
32bit
8bit × 4
10진수
점(.)
```

주소 방식:

```text
Unicast
Multicast
Broadcast
```

---

# IPv4 / IPv6 전환 기술 🔥

## ① 듀얼 스택(Dual Stack)

하나의 장치에서 IPv4와 IPv6를 **둘 다 지원**한다.

```text
IPv4 + IPv6
동시 사용
```

---

## ② 터널링(Tunneling)

IPv6 패킷을 IPv4 네트워크를 통과시키기 위해 IPv4 패킷 내부에 캡슐화한다.

```text
IPv6 패킷
↓
IPv4 안에 캡슐화
↓
전송
```

---

## ③ 주소 변환(Address Translation)

IPv4 주소와 IPv6 주소를 서로 변환하여 두 네트워크를 연결한다.

---

## 암기

```text
둘 다 지원
→ Dual Stack

캡슐화
→ Tunneling

주소 바꾸기
→ Translation
```

---

# 12. UNIX 🔥 빈출

## 문제

다음 설명에 해당하는 운영체제를 적으시오.

1960년대 말 벨 연구소(Bell Labs)에서 개발된 운영체제로 다중 사용자, 멀티태스킹 운영체제이다.

대부분 C언어로 작성되어 있어 이식성이 높다.

트리 구조의 파일 시스템을 갖는다.

## 정답

**UNIX(유닉스)**

---

# UNIX 특징

* Bell Labs에서 개발
* 다중 사용자
* 멀티태스킹
* C언어 기반
* 높은 이식성
* 계층적 / 트리형 파일 시스템

```text
Bell Labs
C언어
다중 사용자
멀티태스킹
트리형 파일 시스템
→ UNIX
```

### 역사 보충

1969년 Ken Thompson을 중심으로 개발되었고 이후 Dennis Ritchie 등이 참여했다.

C언어로 재작성되면서 다른 하드웨어로 옮기기 쉬워졌다.

---

# 13. Hadoop 🔥 빈출

## 문제

다음 설명에 해당하는 용어를 쓰시오.

* 정형 데이터 및 사진, 영상 등의 비정형 데이터를 효과적으로 처리하는 오픈소스 빅데이터 솔루션이다.
* 많은 양의 데이터를 여러 대의 범용 컴퓨터에 나누어 저장하고 처리한다.
* 더그 커팅과 마이크 캐퍼렐라가 개발했으며, 구글의 맵리듀스 관련 기술에 영향을 받아 개발되었다.

## 정답

**Hadoop(하둡)**

---

# Hadoop

대규모 데이터를 여러 컴퓨터에 분산하여 저장하고 처리할 수 있도록 하는 **오픈소스 분산 처리 프레임워크**이다.

### 핵심

```text
빅데이터
오픈소스
분산 저장
분산 처리
→ Hadoop
```

---

# Hadoop 핵심 구성 ⭐

## HDFS

**Hadoop Distributed File System**

데이터를 여러 컴퓨터에 분산 저장한다.

```text
저장
→ HDFS
```

## MapReduce

대규모 데이터를 분산하여 병렬 처리하는 모델이다.

```text
처리
→ MapReduce
```

### 암기

```text
HDFS
→ 저장

MapReduce
→ 처리
```

---

# 빅데이터 관련 용어 🔥

## Big Data

대량의 정형·반정형·비정형 데이터를 처리하고 분석하여 가치를 추출하는 기술 및 데이터 집합.

### 3V

```text
Volume
→ 규모

Velocity
→ 속도

Variety
→ 다양성
```

추가로 Value, Veracity 등을 포함하여 5V로 설명하기도 한다.

---

# NoSQL

기존 관계형 데이터베이스와 다른 방식으로 데이터를 저장하는 비관계형 데이터베이스 계열.

### 특징

* 대규모 데이터
* 유연한 데이터 구조
* 분산 처리
* 높은 확장성

---

# 데이터 마이닝(Data Mining)

대규모 데이터에서 유용한 규칙이나 패턴을 발견하는 기술.

```text
대량 데이터
→ 규칙 / 패턴 발견
```

---

# 데이터 웨어하우스(Data Warehouse)

여러 시스템의 데이터를 통합하여 **분석을 목적으로 장기간 저장하는 데이터 저장소**이다.

```text
여러 시스템 데이터
→ 통합
→ 분석용 저장
```

---

# 데이터 마트(Data Mart)

데이터 웨어하우스 중 특정 부서나 사용 목적에 필요한 데이터만 추출한 작은 규모의 저장소.

```text
Data Warehouse
↓
특정 부서/분야
↓
Data Mart
```

---

# OLAP

**Online Analytical Processing**

사용자가 데이터를 다차원적으로 조회하고 분석하여 의사결정을 지원하는 기술이다.

```text
분석
다차원
의사결정
→ OLAP
```

---

# Mashup

웹에서 제공되는 여러 서비스나 데이터를 결합하여 새로운 서비스나 애플리케이션을 만드는 기술이다.

```text
기존 웹 서비스 + 데이터
→ 새로운 서비스
```

---

# 14. 블록체인(Blockchain) ⭐ 중요

## 문제

다음 설명에 해당하는 용어를 쓰시오.

관리 대상 데이터를 '블록'이라고 하는 소규모 데이터들이 P2P 방식을 기반으로 생성된 체인 형태의 연결고리 기반 분산 데이터 저장 환경에 저장하여 누구라도 임의로 수정할 수 없고 누구나 변경의 결과를 열람할 수 있는 분산 컴퓨팅 기술 기반의 원장 관리 기술

## 정답

**블록체인(Blockchain)**

---

# 블록체인 개념

거래 데이터를 블록 단위로 저장하고 각 블록을 이전 블록과 연결하여 여러 참여자가 공동으로 관리하는 **분산 원장 기술**이다.

```text
Block
↓
Block
↓
Block
```

## 핵심 특징

* 분산 원장
* P2P
* 블록 연결
* 위변조가 어려움
* 참여자들이 기록을 공유

```text
P2P
분산 저장
체인
위변조 방지
→ Blockchain
```

---

# 15. NAT 🔥🔥 매우 빈출

## 문제

다음 설명하는 용어를 쓰시오.

* 우리나라 말로 번역하면 '네트워크 주소 변환'이라는 의미
* 내부에서 사용하는 사설 IP주소와 외부로 보여지는 공인 IP주소 간의 IP Address 변환 방식을 말한다.
* 한정된 공인 IP를 여러 개의 내부 사설 IP에 사용할 수 있도록 하는 기술

## 정답

**NAT(Network Address Translation)**

---

# NAT

사설 IP 주소와 공인 IP 주소를 서로 변환하는 기술이다.

```text
내부 네트워크
192.168.0.10
192.168.0.11
192.168.0.12
      ↓
     NAT
      ↓
공인 IP
```

## 목적

* 공인 IPv4 주소 부족 문제 완화
* 내부 사설망과 외부망 연결
* 내부 IP 주소를 직접 외부에 노출하지 않음

---

# 사설 IP 주소 범위 ⭐ 같이 외우기

```text
10.0.0.0
~
10.255.255.255
```

```text
172.16.0.0
~
172.31.255.255
```

```text
192.168.0.0
~
192.168.255.255
```

시험에서는 특히:

```text
10.x.x.x
172.16 ~ 172.31.x.x
192.168.x.x
```

정도로 기억해두면 좋다.

---

# NAT / PAT

## NAT

IP 주소를 변환한다.

## PAT

**Port Address Translation**

IP 주소뿐 아니라 포트 번호까지 이용하여 **하나의 공인 IP를 여러 내부 호스트가 공유**할 수 있게 한다.

```text
192.168.0.10:5000 ─┐
192.168.0.11:5001 ─┼→ 하나의 공인 IP
192.168.0.12:5002 ─┘
```

실제 가정용 공유기 환경에서 흔히 사용하는 방식이다.

---

# 2020년 4회 중요도 정리

| 번호 | 개념               | 중요도        |
| -- | ---------------- | ---------- |
| 1  | GoF 디자인 패턴       | 🔥🔥 매우 빈출 |
| 2  | UML 다이어그램        | 🔥 빈출      |
| 3  | 샘플링 오라클          | 🔥 빈출      |
| 4  | 동등 분할 테스트        | 🔥🔥 매우 빈출 |
| 5  | GROUP BY / COUNT | 🔥🔥 매우 중요 |
| 6  | 스니핑              | 🔥 빈출      |
| 7  | CIA - 가용성        | 🔥🔥 매우 빈출 |
| 8  | DB 이상 현상         | 🔥🔥 매우 빈출 |
| 9  | DB 회복 기법         | 🔥🔥 매우 중요 |
| 10 | 프로세스 상태          | 🔥 빈출      |
| 11 | IPv6             | 🔥🔥 매우 빈출 |
| 12 | UNIX             | 🔥 빈출      |
| 13 | Hadoop           | 🔥 빈출      |
| 14 | Blockchain       | ⭐ 중요       |
| 15 | NAT              | 🔥🔥 매우 빈출 |

---

# 시험 직전 초압축 암기

## GoF

```text
생성
구조
행위
```

```text
Singleton
→ 생성

Facade
→ 구조

Observer
→ 행위
```

---

## UML

```text
클래스
→ 클래스 관계

패키지
→ 요소 그룹화

배치
→ 하드웨어/네트워크 배치

유스케이스
→ 사용자 기능

활동
→ 업무 흐름

시퀀스
→ 메시지 시간순서

상태 머신
→ 상태 변화
```

---

## 테스트 오라클

```text
모든 입력 정확
→ 참 오라클

일부만 정확
→ 샘플링

일부 정확 + 나머지 추정
→ 휴리스틱

변경 전후 비교
→ 일관성 검사
```

---

## 블랙박스

```text
유효/무효 그룹
→ 동등 분할

경계 근처
→ 경계값 분석
```

---

## SQL

```text
GROUP BY
→ 그룹화

COUNT
→ 개수

AS
→ 별칭
```

```text
WHERE
→ 행 조건

HAVING
→ 그룹 조건
```

---

## 보안

```text
Sniffing
→ 패킷 엿보기
```

```text
CIA

C
→ 기밀성

I
→ 무결성

A
→ 가용성
```

```text
DoS / DDoS
→ 가용성 공격
```

---

## 이상 현상

```text
삽입 이상
갱신 이상
삭제 이상
```

```text
이상 현상 해결
→ 정규화
```

---

## DB 회복

```text
즉시 갱신
→ 즉시 DB 반영
→ Undo + Redo

지연 갱신
→ 완료 후 반영
→ Redo만

검사점
→ Checkpoint 이후 중심 복구

그림자 페이징
→ 로그 X
→ 복사 페이지 사용
```

---

## 프로세스 상태

```text
Ready
→ CPU 기다림

Running
→ CPU 사용 중

Waiting
→ I/O 등 기다림
```

---

## IPv6

```text
128bit
16bit × 8
16진수
:
```

```text
Unicast
Multicast
Anycast
```

```text
Broadcast X
```

### 전환

```text
둘 다 지원
→ Dual Stack

캡슐화
→ Tunneling

주소 변경
→ Translation
```

---

## UNIX

```text
Bell Labs
C언어
다중 사용자
멀티태스킹
트리형 파일 시스템
```

---

## Hadoop

```text
빅데이터
분산 저장
분산 처리
오픈소스
```

```text
HDFS
→ 저장

MapReduce
→ 처리
```

---

## Blockchain

```text
P2P
분산 원장
Block Chain
위변조 어려움
```

---

## NAT

```text
사설 IP
↔
공인 IP
```

```text
IPv4 주소 부족 완화
→ NAT
```

---

# 이번 회차 최우선 암기

```text
1. GoF = 생성 / 구조 / 행위
2. UML 주요 다이어그램 구분
3. 테스트 오라클 4종류
4. 동등 분할 / 경계값 분석
5. CIA = 기밀성 / 무결성 / 가용성
6. 삽입 / 갱신 / 삭제 이상
7. 즉시 갱신 / 지연 갱신
8. IPv4 / IPv6 + 전환 기술
9. NAT
10. GROUP BY / HAVING / 집계 함수
```


