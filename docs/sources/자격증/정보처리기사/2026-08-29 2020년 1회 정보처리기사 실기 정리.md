# 2020년 1회 정보처리기사 실기 정리

## 1. 비정규화(Denormalization, 반정규화, 역정규화)

### 개념

정규화된 데이터베이스의 **성능 향상**과 개발 편의성을 위해 일부러 정규화 원칙에 위배되도록 데이터 구조를 변경하는 기법이다.

### 실기 답안

**데이터베이스 정규화 이후 성능 향상과 개발 편의성을 위해 정규화 원칙에 위배되도록 수행하는 기법**

> 핵심 키워드: **정규화 이후 / 성능 향상**

---

# 2. XML

### 문제 핵심

* SGML의 단순화된 부분집합
* 서로 다른 시스템 간 데이터 교환
* HTML의 한계를 보완
* 유니코드 지원

### 정답

**XML**

### XML

**eXtensible Markup Language**

웹에서 구조화된 문서를 표현하고 전송하기 위해 설계된 마크업 언어이다.

---

## 인터페이스 전송 데이터 형식

### JSON

**JavaScript Object Notation**

* JavaScript 객체 문법을 기반으로 구조화된 데이터를 표현하는 문자 기반 표준 형식
* **이름과 값의 쌍(Key-Value)**으로 구성
* XML보다 비교적 간결하고 용량이 작음

예:

```json
{
  "name": "홍길동",
  "age": 20
}
```

### XML

**eXtensible Markup Language**

* 태그를 이용하여 구조화된 데이터를 표현
* 시스템 간 데이터 교환에 사용

예:

```xml
<student>
    <name>홍길동</name>
    <age>20</age>
</student>
```

### CSV

**Comma Separated Values**

* 여러 필드를 쉼표(,)로 구분하는 텍스트 데이터 형식

예:

```text
홍길동,20,컴퓨터공학과
```

### YAML

* 사람이 읽기 쉽게 만들어진 데이터 직렬화 형식
* 설정 파일 등에서 많이 사용

예:

```yaml
name: 홍길동
age: 20
```

---

# 3. 살충제 패러독스(Pesticide Paradox)

### 실기 답안

**동일한 테스트 케이스를 반복적으로 수행하면 새로운 결함을 발견하기 어려워진다는 원리이다.**

즉, 새로운 결함을 찾기 위해서는 테스트 케이스를 지속적으로 검토하고 개선해야 한다.

---

## 소프트웨어 테스트의 7가지 원리

1. **테스팅은 결함이 존재함을 밝히는 활동이다.**
2. **완벽한 테스팅은 불가능하다.**
3. **테스팅은 개발 초기에 시작해야 한다.**
4. **결함 집중(Defect Clustering)**

   * 결함은 특정 모듈에 집중되어 발생한다.
   * 파레토 법칙과 관련됨.
5. **살충제 패러독스**

   * 같은 테스트를 반복하면 새로운 결함을 찾기 어렵다.
6. **테스팅은 정황(Context)에 의존한다.**
7. **오류-부재의 궤변**

   * 오류가 없더라도 사용자의 요구사항을 만족하지 못하면 좋은 소프트웨어가 아니다.

---

# 4. 결합도와 응집도

### 문제

모듈의 독립성을 판단하는 두 가지 지표

### 정답

1. **결합도(Coupling)**
2. **응집도(Cohesion)**

### 핵심

좋은 모듈 설계:

**결합도는 낮게 ↓**

**응집도는 높게 ↑**

---

# 결합도(Coupling)

모듈과 모듈 사이의 **상호 의존 정도**

### 좋은 순서 → 나쁜 순서

**자료 → 스탬프 → 제어 → 외부 → 공통 → 내용**

즉,

```text
자료 < 스탬프 < 제어 < 외부 < 공통 < 내용
```

결합도는 **낮을수록 좋다.**

---

## ① 자료 결합도(Data Coupling)

모듈 간에 필요한 **단순 데이터 값만 전달**하는 경우

예:

```java
sum(10, 20);
```

가장 좋은 결합도

---

## ② 스탬프 결합도(Stamp Coupling)

배열, 객체, 구조체 등의 **자료구조 전체를 전달**하는 경우

예:

```java
printStudent(student);
```

---

## ③ 제어 결합도(Control Coupling)

다른 모듈의 처리 방법을 결정하는 **제어 정보**를 전달하는 경우

예:

```text
flag = 1이면 등록
flag = 2이면 삭제
```

---

## ④ 외부 결합도(External Coupling)

두 모듈이 외부에서 도입된 데이터 형식, 통신 프로토콜, 장치 인터페이스 등을 공유하는 경우

---

## ⑤ 공통 결합도(Common Coupling)

여러 모듈이 **전역 변수와 같은 공통 데이터 영역을 공유**하는 경우

---

## ⑥ 내용 결합도(Content Coupling)

한 모듈이 다른 모듈의 **내부 데이터나 내부 기능을 직접 참조하거나 변경**하는 경우

가장 나쁜 결합도

---

# 응집도(Cohesion)

한 모듈 내부의 구성 요소들이 **얼마나 밀접하게 관련되어 있는지**를 나타내는 정도

### 좋은 순서 → 나쁜 순서

```text
기능적 > 순차적 > 통신적 > 절차적 > 시간적 > 논리적 > 우연적
```

응집도는 **높을수록 좋다.**

---

## ① 기능적 응집도(Functional Cohesion)

모듈의 모든 기능이 **하나의 목적을 위해 수행**되는 경우

가장 좋은 응집도

---

## ② 순차적 응집도(Sequential Cohesion)

한 활동의 **출력값이 다음 활동의 입력값**으로 사용되는 경우

```text
A의 결과 → B의 입력
```

---

## ③ 통신적 응집도(Communication Cohesion)

동일한 입력이나 출력을 사용하는 여러 기능들이 하나의 모듈에 존재하는 경우

---

## ④ 절차적 응집도(Procedural Cohesion)

여러 기능이 **정해진 순서에 따라 수행**되는 경우

> 주의
> 단순히 "순서대로 실행된다"는 설명이면 **절차적 응집도**일 가능성이 높다.

---

## ⑤ 시간적 응집도(Temporal Cohesion)

특정 시점에 함께 수행되어야 하는 기능들이 모인 경우

예:

```text
프로그램 시작 시 초기화
프로그램 종료 시 정리
```

---

## ⑥ 논리적 응집도(Logical Cohesion)

유사한 성격이나 같은 범주의 기능들을 하나의 모듈에 모아둔 경우

---

## ⑦ 우연적 응집도(Coincidental Cohesion)

모듈 내부의 기능들이 서로 아무런 관련이 없는 경우

가장 나쁜 응집도

---

# 6. JSON

### 문제 핵심

* JavaScript 객체 문법
* 문자 기반 표준 포맷
* 사람과 기계 모두 이해하기 쉬움
* XML보다 간결함
* 이름과 값의 쌍

### 정답

**JSON**

### 실기용 정의

**JavaScript 객체 문법을 기반으로 구조화된 데이터를 표현하기 위한 문자 기반의 표준 데이터 형식**

---

# 7. 애플리케이션 성능 측정 지표

## ① 처리량(Throughput)

**일정 시간 동안 처리할 수 있는 작업의 양**

---

## ② 응답 시간(Response Time)

**요청을 전달한 시점부터 응답이 시작되거나 도착할 때까지 걸리는 시간**

---

## ③ 경과 시간 / 반환 시간(Turnaround Time)

**요청을 전달한 시점부터 전체 처리가 완료될 때까지 걸리는 시간**

---

## ④ 자원 사용률(Resource Usage)

작업을 수행하면서 사용되는 자원의 양

* CPU 사용량
* 메모리 사용량
* 네트워크 사용량

---

# 8. SQL DISTINCT

학생 데이터

* 전자과: 50명
* 전기과: 100명
* 건축과: 50명

총 학생 수 = **200명**

---

### ①

```sql
SELECT dept
FROM STUDENT;
```

결과 행 수:

**200**

---

### ②

```sql
SELECT DISTINCT dept
FROM STUDENT;
```

학과 종류만 출력

결과 행 수:

**3**

---

### ③

```sql
SELECT COUNT(DISTINCT dept)
FROM STUDENT
WHERE dept = '건축과';
```

결과:

```text
1
```

그리고 `COUNT()`는 집계 함수이므로 **결과 튜플 자체도 1개**이다.

---

# 9. LAND Attack

### 문제 특징

* 출발지 IP = 목적지 IP
* 자기 자신에게 SYN 요청이 반복됨
* 서버 자원 고갈

### 정답

**LAND Attack**

### 실기용 정의

**출발지 IP와 목적지 IP를 공격 대상의 IP 주소로 동일하게 설정한 패킷을 보내 서버가 자기 자신에게 응답하도록 만들어 자원을 고갈시키는 공격**

---

# 서비스 거부 및 보안 공격 유형 정리

## DoS

**Denial of Service**

하나의 공격자가 대량의 트래픽이나 비정상적인 요청을 보내 시스템 자원을 고갈시켜 정상적인 서비스를 방해하는 공격

---

## DDoS

**Distributed Denial of Service**

여러 대의 감염된 시스템을 이용하여 동시에 공격 대상에 대량의 트래픽을 보내 서비스를 마비시키는 공격

---

## Smurf Attack

공격 대상의 IP를 출발지 주소로 위조한 ICMP 요청을 브로드캐스트 주소로 보내 다수의 응답이 공격 대상에게 전달되도록 하는 공격

---

## SYN Flooding

TCP의 3-Way Handshake 과정에서 대량의 SYN 패킷을 보내고 최종 ACK를 보내지 않아 서버의 연결 자원을 고갈시키는 공격

---

## UDP Flooding

대량의 UDP 패킷을 전송하여 서버와 네트워크 자원을 고갈시키는 공격

---

## Ping Flooding

대량의 ICMP Echo Request(Ping)를 보내 네트워크와 시스템 자원을 고갈시키는 공격

---

## Ping of Death

허용 크기를 초과한 비정상적인 ICMP 패킷을 전송하여 시스템에 장애를 발생시키는 공격

---

## Teardrop Attack

IP 패킷의 Fragment Offset 값을 조작하여 패킷 재조립 과정에서 오류를 발생시키는 공격

---

## LAND Attack

출발지 IP와 목적지 IP를 동일하게 만들어 공격 대상이 자기 자신에게 응답하도록 하는 공격

---

## Phishing

가짜 이메일이나 웹사이트를 이용하여 사용자를 속이고 개인정보나 금융정보를 탈취하는 공격

---

## Pharming

DNS 등을 조작하여 사용자가 정상 주소를 입력해도 가짜 사이트로 접속하도록 유도하는 공격

---

## Sniffing

네트워크를 통해 전송되는 패킷을 가로채어 정보를 훔쳐보는 공격

---

## Smishing

SMS 문자메시지를 이용한 피싱 공격

---

## Qshing / QR Phishing

악성 QR 코드를 이용해 피싱 사이트나 악성 페이지로 유도하는 공격

---

## Ransomware

파일이나 시스템을 암호화한 뒤 복구를 조건으로 금전을 요구하는 악성코드

---

## Key Logger

사용자가 키보드로 입력하는 정보를 몰래 기록하여 계정이나 비밀번호 등을 탈취하는 공격

---

## SQL Injection

SQL 쿼리에 악의적인 SQL 문을 삽입하여 데이터베이스 정보를 조회·변조·삭제하는 공격

---

## XSS

**Cross Site Scripting**

웹페이지에 악성 스크립트를 삽입하여 다른 사용자의 브라우저에서 실행되도록 하는 공격

---

## Brute Force Attack

**무차별 대입 공격**

가능한 비밀번호나 암호 조합을 반복적으로 대입하여 인증 정보를 알아내는 공격

---

## Spear Phishing

특정 개인이나 조직을 대상으로 맞춤형 메시지를 보내 정보를 탈취하는 공격

---

## APT

**Advanced Persistent Threat**

특정 조직을 대상으로 장기간에 걸쳐 지속적이고 은밀하게 수행되는 지능형 공격

---

## Zero-Day Attack

보안 취약점이 발견되었지만 아직 보안 패치가 제공되지 않은 상태에서 해당 취약점을 이용하는 공격

---

## Back Door

정상적인 인증 과정을 우회하여 시스템에 접근할 수 있도록 만들어 놓은 비정상적인 접근 통로

---

## Rainbow Table Attack

미리 계산해 둔 해시값과 평문 목록을 이용하여 암호화된 비밀번호의 원문을 찾아내는 공격

---

## CSRF

**Cross Site Request Forgery**

로그인된 사용자의 권한을 이용하여 사용자가 의도하지 않은 요청을 서버에 수행하게 만드는 공격

---

## TOCTOU

**Time Of Check To Time Of Use**

자원이나 파일의 상태를 확인한 시점과 실제 사용 시점 사이의 시간차를 이용하는 공격

---

# 10. 통신 프로토콜의 기본 요소 3가지

## ① 구문(Syntax)

데이터의 구조, 형식, 부호화 방법 등을 정의

---

## ② 의미(Semantics)

데이터의 각 부분이 무엇을 의미하는지와 어떤 동작을 수행할지를 정의

---

## ③ 타이밍(Timing)

데이터를 언제 보내고 어느 정도의 속도로 전송할지를 정의

---

### 암기

```text
구 - 의 - 타
구문 / 의미 / 타이밍
```

---

# 11. HRN 스케줄링

## HRN

**Highest Response Ratio Next**

### 우선순위 계산식

```text
(대기시간 + 서비스시간) / 서비스시간
```

또는

```text
1 + (대기시간 / 서비스시간)
```

**값이 큰 프로세스부터 실행**

---

# 프로세스 스케줄링

## 비선점 스케줄링

현재 실행 중인 프로세스로부터 CPU를 강제로 빼앗지 않는 방식

* FCFS
* SJF
* HRN
* 우선순위
* 기한부

### 암기

**F S H 우 기**

---

## 선점 스케줄링

우선순위가 높은 프로세스 등이 CPU를 강제로 가져올 수 있는 방식

* SRT
* RR
* MLQ
* MFQ

---

# 12. 트랜잭션의 4가지 특성 ACID

## ① 원자성(Atomicity)

트랜잭션의 작업은 **모두 수행되거나 모두 수행되지 않아야 한다.**

```text
All or Nothing
```

---

## ② 일관성(Consistency)

트랜잭션 수행 전후 데이터베이스는 항상 일관된 상태를 유지해야 한다.

---

## ③ 독립성 / 격리성(Isolation)

하나의 트랜잭션이 실행되는 동안 다른 트랜잭션의 영향을 받지 않아야 한다.

---

## ④ 영속성 / 지속성(Durability)

성공적으로 완료된 트랜잭션의 결과는 영구적으로 저장되어야 한다.

---

### 암기

```text
원 일 독 영
Atomicity
Consistency
Isolation
Durability
```

= **ACID**

---

# 13. OSI 7계층

### 문제 특징

* 전기적·기계적·기능적 특성
* 비트 단위
* 0과 1
* 데이터 내용이나 오류는 고려하지 않음

### 정답

**물리 계층(Physical Layer)**

---

# OSI 7계층

```text
7 응용
6 표현
5 세션
4 전송
3 네트워크
2 데이터링크
1 물리
```

### 아래에서 위로 암기

```text
물 데 네 전 세 표 응
```

---

## 주요 장비

### 1계층 물리

* 허브(Hub)
* 리피터(Repeater)

### 2계층 데이터링크

* 브리지(Bridge)
* 스위치(Switch)

### 3계층 네트워크

* 라우터(Router)

---

## ARP / RARP

### ARP

**IP 주소 → MAC 주소**

```text
IP → MAC
```

### RARP

**MAC 주소 → IP 주소**

```text
MAC → IP
```

---

# 14. 릴리즈 노트 작성 항목

문서 이름, 제품 이름, 버전 번호, 릴리즈 날짜, 참고 날짜, 노트 버전 등을 기록하는 부분

### 정답

**헤더(Header)**

→ 암기 문제

---

# 15. 데이터 마이닝(Data Mining)

### 실기 답안

**대량의 데이터에서 통계적·수학적 기법 등을 이용하여 유용한 정보나 규칙, 패턴을 찾아내는 과정**

또는

**대규모로 저장된 데이터에서 가치 있는 정보와 패턴을 자동으로 발견하는 기술**

### 핵심

```text
대량의 데이터 → 숨겨진 패턴/규칙 → 유용한 정보
```

---

# 16. MD5

### 문제 특징

* R. Rivest
* MD4 개선
* 일방향 해시 함수
* 128비트 해시값

### 정답

**MD5**

---

# 암호화 알고리즘 정리

## ① 대칭키 암호화

암호화 키와 복호화 키가 동일

* DES
* AES
* ARIA
* SEED
* IDEA

### 특징

```text
속도 빠름
키 관리가 어려움
```

---

## ② 비대칭키 / 공개키 암호화

암호화 키와 복호화 키가 다름

* RSA
* ElGamal
* ECC

### 특징

```text
공개키 + 개인키 사용
속도가 상대적으로 느림
키 관리가 쉬움
```

---

## ③ 해시 알고리즘

입력값을 일정한 길이의 해시값으로 변환하는 **일방향 함수**

* MD5
* SHA 계열
* HAS-160

> MD5는 128비트이며 현재는 충돌 취약점 때문에 보안 목적으로는 권장되지 않는다.

---

# 시험 직전 초압축 암기

## 비정규화

**정규화 이후 성능 향상을 위해 일부러 정규화 원칙을 위배**

## XML

**구조화된 문서를 표현·전송하는 마크업 언어**

## 살충제 패러독스

**같은 테스트 반복 → 새로운 결함 발견 어려움**

## 모듈 독립성

**결합도 ↓ / 응집도 ↑**

## 결합도

```text
자료-스탬프-제어-외부-공통-내용
```

## 응집도

```text
기능-순차-통신-절차-시간-논리-우연
```

## JSON

**Key-Value 기반 데이터 표현 형식**

## 성능 지표

```text
처리량
응답시간
경과시간
자원사용률
```

## LAND

**출발지 IP = 목적지 IP**

## 프로토콜

```text
구문 / 의미 / 타이밍
```

## HRN

```text
(대기시간 + 실행시간) / 실행시간
```

## ACID

```text
원자성 / 일관성 / 독립성 / 영속성
```

## 물리 계층

```text
Bit / Hub / Repeater
```

## ARP

```text
IP → MAC
```

## RARP

```text
MAC → IP
```

## 릴리즈 노트

**헤더**

## 데이터 마이닝

**대량 데이터에서 유용한 패턴·정보 추출**

## MD5

```text
128bit 해시
```

---

# 특히 헷갈리기 쉬운 부분

### 1. 결합도

**낮을수록 좋음**

```text
자료 ← 가장 좋음
내용 ← 가장 나쁨
```

### 2. 응집도

**높을수록 좋음**

```text
기능적 ← 가장 좋음
우연적 ← 가장 나쁨
```

### 3. 순차적 vs 절차적 응집도

**순차적**

```text
앞 작업의 출력값 → 다음 작업의 입력값
```

**절차적**

```text
여러 작업을 단순히 정해진 순서대로 수행
```

### 4. 응답시간 vs 경과시간

**응답시간**

```text
요청 → 응답
```

**경과시간**

```text
요청 → 전체 작업 완료
```

### 5. ARP / RARP

```text
ARP  : IP → MAC
RARP : MAC → IP
```

### 6. 원자성

```text
All or Nothing
```

### 7. HRN

```text
(대기 + 실행) / 실행
값이 클수록 먼저 실행
```
