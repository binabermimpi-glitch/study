# 개발 공부 LLM Wiki 운영 규칙

## 목적

이 폴더는 프로그래밍 비전공·입문자가 자바 백엔드 국비 과정을 따라가고, 정보처리기사·SQLD·리눅스마스터 2급을 준비하며, 이후 실무 개발자로 성장하기 위한 개인 지식 위키다.

Codex의 역할은 사용자가 넣은 수업 자료와 메모를 보존하면서, 내용을 이해하기 쉬운 위키 문서로 축적하고 연결하는 것이다.

## 작업 범위와 안전 규칙

- 이 `개발 공부 LLM Wiki/` 폴더 안에서만 파일을 생성·수정한다.
- Vault의 다른 기존 노트는 사용자가 명시적으로 허락하기 전까지 수정하거나 이동하지 않는다.
- `10 원문/`의 파일은 원문이므로 절대 덮어쓰거나 내용을 고치지 않는다.
- 원문을 정리해야 할 때는 `20 위키/03 출처 노트/`에 별도의 출처 노트를 만든다.
- 삭제, 대량 이동, 대량 이름 변경은 먼저 사용자에게 변경 목록을 설명하고 확인받는다.
- 확신할 수 없는 내용은 사실처럼 쓰지 말고 `확인 필요`로 표시한다.

## 폴더 역할

- `00 인박스/`: 아직 분류·처리하지 않은 메모와 자료
- `10 원문/`: 수업자료, 과제, 기사, 책 메모 등 보존 대상 원문
- `20 위키/01 개념/`: 하나의 용어나 원리를 설명하는 문서. Java, Spring, Web, Database, CS, Tools 영역으로 나눈다.
- `20 위키/02 주제/`: 여러 개념을 연결한 학습 단원 또는 종합 문서. Java, Spring, Database, Backend 영역으로 나눈다.
- `20 위키/03 출처 노트/`: 원문 하나의 요약, 신뢰도, 관련 개념을 기록
- `20 위키/04 자격증/`: 정보처리기사, SQLD, 리눅스마스터 2급을 각각 하나의 누적 문서로 관리
- `20 위키/05 문제 풀이/`: 국비수업에서 푼 문제, 첫 시도, 정답과 재풀이 이력을 관리
- `90 템플릿/`: 새 위키 문서를 만들 때 사용하는 양식

## 지식 분류 기준

모든 개념·주제 문서에는 아래 `knowledge_type` 중 하나를 지정한다.

- `foundation`: 자바와 백엔드를 이해하기 위한 기본 배경지식. 당장 코드에서 자주 보이지 않아도 이후 학습의 토대가 되는 것
- `practical-core`: 현업에서 자주 사용하거나 채용·과제·프로젝트에서 직접 요구되는 실무 핵심지식
- `limited-use`: 특정 상황, 레거시, 시험 또는 이론 이해에는 필요하지만 일반적인 실무에서 직접 사용할 빈도가 낮은 지식
- `unclassified`: 아직 판단할 근거가 부족한 지식

분류에는 이유를 한 문장으로 적는다. `limited-use`라고 해서 불필요하다는 뜻으로 표현하지 않는다.

## 문서 작성 원칙

- 프로그래밍을 처음 배우는 사람이 이해할 수 있는 쉬운 말로 먼저 설명한다.
- 전문용어는 처음 등장할 때 뜻을 풀어 쓰고 관련 개념 문서에 Obsidian 위키 링크를 건다.
- 정의만 적지 말고 `왜 필요한가 → 어떻게 동작하는가 → 예시 → 흔한 실수` 순서로 설명한다.
- 코드 예제는 가능한 한 작고 실행 가능한 형태로 작성한다.
- 자바 버전, Spring Boot 버전 등 버전에 따라 달라지는 내용은 적용 버전을 적는다.
- 하나의 개념은 하나의 대표 문서에서 관리하고, 중복 문서를 만들기 전에 검색한다.
- Java 언어 자체의 문법·표준 API·JVM 지식은 `Java/`에, Spring 프레임워크의 기능과 사용법은 `Spring/`에 저장한다.
- HTTP나 REST처럼 Spring에 종속되지 않는 지식은 `Web/`, SQL과 데이터 모델링은 `Database/`에 저장한다.
- 여러 영역에 걸친 내용은 가장 중심이 되는 영역에 한 번만 저장하고 다른 문서에서 링크한다.
- 자격증에서 배운 일반 개념은 `04 자격증/`에 중복 작성하지 않고 `01 개념/`의 기존 문서를 만들거나 보강한다.
- `04 자격증/`에는 자격증마다 하나의 대표 문서만 두고 학습 내용을 공부한 순서대로 누적한다.
- 국비수업과 자격증에서 같은 개념을 다루면 하나의 개념 문서에 양쪽 출처를 모두 연결한다.
- 동일 개념 문서에서 `국비수업 연결`, `자격증 시험 포인트`, `실무에서는 어떻게 쓰이나`를 구분한다.
- 자격증 자료를 반영할 때 해당 자격증 대표 문서와 기존 Java·Spring·Web·Database·CS·Linux 문서를 먼저 검색한다.
- 같은 자격증 개념이 다시 들어오면 동일 설명을 새로 복사하지 않는다.
- 반복된 개념의 기존 항목에 `반복 확인 날짜`, `반복 횟수`, `아직 헷갈리는 부분`을 갱신한다.
- 새로운 설명이나 교정 내용이 있을 때만 기존 항목의 본문을 보강한다.
- 자격증 대표 문서의 항목은 최초 학습 날짜 순서로 배치한다.
- 국비수업 문제 풀이는 개념 문서와 분리하되 관련 개념 문서에 양방향으로 연결한다.
- 학습자가 처음 작성한 코드와 풀이 과정은 틀렸더라도 수정하거나 삭제하지 않고 그대로 보존한다.
- 정답 코드는 첫 풀이와 구분되는 별도 섹션에 작성한다.
- 문제를 틀린 원인은 `개념 부족`, `문법 기억`, `문제 해석`, `코드 조립`, `실행 결과 예측`, `단순 실수` 중에서 선택하거나 추가한다.
- 한 번 정답을 확인한 문제는 즉시 완료 처리하지 않고, 정답 없이 다시 푼 기록이 있어야 `solved: true`로 변경한다.
- 선행지식과 다음 학습 항목을 실제 문서로 연결되는 Obsidian 위키 링크로 연결한다.
- 원문에서 얻은 주장에는 실제 출처 노트 문서로 연결되는 위키 링크를 단다.
- Codex의 해석과 원문에 명시된 내용을 구분한다.
- 기존 설명과 새 원문이 충돌하면 한쪽을 지우지 말고 `## 충돌 및 확인 필요`에 양쪽 근거를 기록한다.

## 인박스 처리 절차

1. 처리 대상과 사용자의 학습 목적을 확인한다.
2. 원본을 `10 원문/`에 보존한다. 이미 원문 폴더에 있다면 복제하지 않는다.
3. `90 템플릿/출처 노트 템플릿.md`를 바탕으로 출처 노트를 만든다.
4. 기존 `20 위키/index.md`와 관련 문서를 먼저 검색한다.
5. 새 개념·주제 문서를 만들거나 기존 문서를 보강한다.
6. 모든 새 문서에 출처, 선행지식, 지식 분류와 분류 이유를 기록한다.
7. `20 위키/index.md`를 갱신한다.
8. `20 위키/log.md`에 처리 내역을 추가한다.
9. 처리 결과, 중요 포인트, 확인이 필요한 부분을 사용자에게 알려준다.

## 자격증 자료 처리 절차

1. 정보처리기사, SQLD, 리눅스마스터 2급 중 어느 시험의 자료인지 확인한다.
2. 원문을 해당 `10 원문/자격증/` 하위 폴더에 보존한다.
3. `정보처리기사.md`, `SQLD.md`, `리눅스마스터 2급.md` 중 해당 대표 문서를 검색한다.
4. 처음 등장한 내용이면 대표 문서 마지막에 최초 학습 날짜와 함께 새 항목을 추가한다.
5. 기존에 있던 내용이면 본문을 반복하지 않고 기존 항목의 반복 날짜와 횟수를 갱신한다.
6. 사용자가 계속 모르는 부분은 `아직 헷갈리는 부분`에 누적하고, 해결되면 상태를 변경한다.
7. 국비수업에서 이미 배운 내용이면 관련 개념 링크를 한 번만 연결한다.
8. 필요할 때만 `01 개념/` 문서를 생성하거나 보강한다.
9. 처리 결과에서 새 내용과 반복된 내용을 구분해 알려준다.

## 국비수업 문제 처리 절차

1. 문제 원문, 조건, 학습자의 첫 코드와 사고 과정을 수정 없이 기록한다.
2. Java, Spring, Database, Web, 종합 프로젝트 중 알맞은 폴더를 선택한다.
3. 문제에서 요구하는 입력, 출력, 제약조건과 핵심 문법을 분리한다.
4. 막힌 지점과 오류 유형을 구체적으로 진단한다.
5. 정답 또는 개선된 코드를 별도 섹션에 작성하고 코드가 왜 동작하는지 설명한다.
6. 관련된 `01 개념/` 문서를 연결하고, 필요한 개념 문서가 없으면 생성한다.
7. 정답을 보지 않고 다시 풀 날짜와 완료 기준을 정한다.
8. `20 위키/05 문제 풀이/국비수업/문제 풀이 현황.md`와 `log.md`를 갱신한다.
9. 첫 시도와 재풀이를 비교하여 반복되는 약점을 기록한다.

## 질문에 답하는 절차

1. `20 위키/index.md`에서 관련 문서를 찾는다.
2. 관련 위키 문서와 출처 노트를 확인한다.
3. 위키에 근거가 충분하면 내부 링크를 인용해 답한다.
4. 근거가 부족하면 부족하다고 밝히고, 웹 검색이나 새 자료 추가가 필요한지 제안한다.
5. 재사용 가치가 높은 답변은 사용자 동의를 받아 주제 문서로 저장한다.

## 위키 점검 절차

점검 요청을 받으면 다음을 확인한다.

- 링크가 없는 고립 문서
- 존재하지 않는 문서를 가리키는 링크
- 중복되거나 이름만 다른 개념
- 출처 없는 핵심 주장
- `unclassified`로 남은 문서
- 선행지식이 누락된 문서
- 새 자료와 충돌하거나 오래된 설명
- 학습 순서상 빠진 연결 고리

## 파일명과 메타데이터

- 파일명은 사람이 검색하기 쉬운 한국어 명칭을 우선하되, Java·HTTP·Spring처럼 통용되는 영문 표기는 유지한다.
- 날짜는 `YYYY-MM-DD` 형식으로 작성한다.
- YAML frontmatter는 템플릿에 정의된 키를 유지한다.
- `status`는 `seed`, `growing`, `reviewed` 중 하나를 사용한다.
- `difficulty`는 `입문`, `초급`, `중급`, `고급` 중 하나를 사용한다.
- 자격증 대표 문서는 `type: certification-note`, `certification`, `started`, `last_updated` 메타데이터를 사용한다.
- 국비수업 문제 문서는 `type: course-problem`, `area`, `result`, `error_type`, `linked_concepts`, `review_date`, `attempt_count`, `solved` 메타데이터를 사용한다.
