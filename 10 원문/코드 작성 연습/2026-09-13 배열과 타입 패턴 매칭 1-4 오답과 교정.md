# 1~4(3) 오답 + 교정 정리

## 1. Object가 int[]이면 짝수 출력

### 내가 쓴 코드

```java
public static void printEven(Object obj)

if(obj instanceof int[] cr){

    for(cr : ar){

        if(ar%2==0){
```

### 정답

```java
public static void printEven(Object obj) {
    if (obj instanceof int[] cr) {
        for (int value : cr) {
            if (value % 2 == 0) {
                System.out.println(value);
            }
        }
    }
}
```

## 2. Object가 String[]이면 길이 5 이상 출력

### 내가 쓴 코드

```java
public static void printLongword(Object obj){
    if (obj instanceof String[] ar){
        for(String s : ar){
            if(ar.length()>=5;
            System.out.println(ar)
        }
        else{
            System.out.print("문자열배열이 아닙니다")
        }
    }
```

### 정답

```java
public static void printLongWord(Object obj) {
    if (obj instanceof String[] ar) {
        for (String s : ar) {
            if (s.length() >= 5) {
                System.out.println(s);
            }
        }
    } else {
        System.out.println("문자열 배열이 아닙니다.");
    }
}
```

## 3. Object가 Cat이면 sleep 호출

### 내가 쓴 코드

```java
class AnimalMain{
    public static void main(String [] args){
        Object obj = new Cat();
        if (obj instanceof cat b){
            b.sleep();
        }
```

### 정답

```java
class Animal { }

class Cat extends Animal {
    public void sleep() {
        System.out.println("잡니다.");
    }
}

public class AnimalMain {
    public static void main(String[] args) {
        Object obj = new Cat();
        if (obj instanceof Cat b) {
            b.sleep();
        }
    }
}
```

## 4. switch 패턴 매칭으로 Object 구분

### 내가 쓴 코드

```java
public static checkType(Object obj({
    switch(obj){
        if(obj instanceof Integer i&&obj>=0){
            System.out.print("큰 정수")
        else(obj instance ...
```

### 정답

```java
public static void checkType(Object obj) {
    switch (obj) {
        case Integer i when i >= 100 -> System.out.println("큰 정수");
        case Integer i -> System.out.println("정수");
        case String s -> System.out.println("문자열");
        case null -> System.out.println("null");
        default -> System.out.println("기타");
    }
}
```

## 핵심

- 배열 전체와 배열 요소 하나를 구분한다.
- 배열 길이는 `ar.length`, 문자열 길이는 `s.length()`다.
- Java는 클래스 이름의 대소문자를 구분한다.
- switch 패턴 매칭에서는 `case 타입 변수 when 조건 ->` 형태를 사용한다.
